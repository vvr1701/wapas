"""Merchant world simulator: seeds real Razorpay test-mode objects, injects failure
events from the real error taxonomy, and holds the hidden customer behavior model
(frozen outside Phase 1 — FR-1.4). The agent never imports from here except via
normalized events."""
